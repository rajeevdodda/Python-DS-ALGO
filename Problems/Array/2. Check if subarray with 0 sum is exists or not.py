def sub_array_sum_naive(sample_array, array_sum):
    for i in range(sample_array):
        sample_sum_set = set()
        sum = array_sum
        for j in range(i+1, len(sample_array)):


            pass
    pass
